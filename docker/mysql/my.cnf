[mysqld]
# Basic Settings (MySQL 8.0 compatible)
default_authentication_plugin = mysql_native_password

# Performance optimizations for SchoolSavvy SaaS
innodb_buffer_pool_size = 256M
innodb_flush_log_at_trx_commit = 2
innodb_flush_method = O_DIRECT

# Connection settings optimized for web application
max_connections = 100
connect_timeout = 60
wait_timeout = 28800
interactive_timeout = 28800
max_allowed_packet = 32M

# Character set for Laravel/PHP applications
character_set_server = utf8mb4
collation_server = utf8mb4_unicode_ci

# SQL Mode for Laravel compatibility
sql_mode = STRICT_TRANS_TABLES,NO_ZERO_DATE,NO_ZERO_IN_DATE,ERROR_FOR_DIVISION_BY_ZERO

# Security
local_infile = 0

# Basic InnoDB Settings
innodb_file_per_table = 1

# Logging (simplified)
log_error = /var/log/mysql/error.log
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2
